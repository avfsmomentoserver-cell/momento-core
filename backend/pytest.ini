[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    slow: resampling-heavy tests, deselect with '-m "not slow"'
    tape: requires the Eagle Eye export in the repository root
