# State Management

## State Layers
1. **Server State** - React Query for API caching
2. **Global State** - Zustand for app-wide state
3. **Local State** - useState for component state
4. **URL State** - React Router for navigation state

## Real-time Updates
- WebSocket context for live data
- Optimistic updates for better UX
- Automatic reconnection with backoff
